{
  "name": "tqx-ts",
  "version": "0.4.0",
  "private": true,
  "license": "GPL-3.0-only",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "bump-version": "lerna version --no-git-tag-version",
    "build": "bun run --env-file=.env --env-file=.env.prod --filter '@tqx-ai/*' build",
    "dev": "bun run --env-file=.env --env-file=.env.dev tsdown --workspace --out-dir output --watch",
    "format": "oxfmt --threads=4 .",
    "format:check": "oxfmt --threads=4 --check .",
    "lint": "oxlint --deny-warnings .",
    "pack:check": "bun pm pack --cwd packages/sdk --dry-run && bun pm pack --cwd packages/cli --dry-run && bun run test:package",
    "publish:cli": "bun run build && bun publish --cwd packages/cli --access public --tag latest",
    "publish:sdk": "bun run build && bun publish --cwd packages/sdk --access public --tag latest",
    "release:check": "bun run release:preflight && bun run test && bun run pack:check",
    "release:preflight": "bun --env-file=.env --env-file=.env.prod scripts/release-preflight.ts",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:smoke": "bun scripts/smoke.ts",
    "test:package": "bun scripts/package-smoke.ts",
    "test": "bun run format:check && bun run lint && bun run typecheck && bun run test:unit && bun run build && bun run test:smoke"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "lerna": "^9.0.7",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "tsdown": "^0.22.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "packageManager": "bun@1.3.14"
}
